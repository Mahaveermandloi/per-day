
  # Generate Expense Tracker UI

  This is a code bundle for Generate Expense Tracker UI. The original project is available at https://www.figma.com/design/B5C1SsPb2C6mvzZy2laOhq/Generate-Expense-Tracker-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  